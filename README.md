# DallE-1
PyTorch implementation of the DALL·E-1 text-to-image generation model that trains and generates images on a synthetic dataset containing colored MNIST digits with textured and solid backgrounds
